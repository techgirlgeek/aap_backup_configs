namespace: aap
schedule: "* */12 * * *" # every 12 hours
backupFilenamePrefix: export-results
storageSize: 1Gi
# Replace the controllerHost value with your controller host URL. Below is just for demo purposes
controllerHost: "https://aap-aap.apps.cluster-vrcnk-1.dynamic.redhatworkshops.io/"
controllerUsername: "admin"
# Name of the operator-generated secret that contains the controller admin password
controllerAdminSecretName: "aap-controller-admin-password"
